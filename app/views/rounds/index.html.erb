<div class="container">
  <div class="row">
    <div class="col">
      <p>ランク</p>
      <p>スコア　パット</p>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-9">
      グラフ
      <% if @rounds.present? %>
      <table>
        <tbody>
          <tr>
            <th>day</th>
            <% @rounds.each do |round| %>
              <td><%= round.date %></td>
            <% end %>
          </tr>
          <tr>
            <th>score</th>
            <% @rounds.each do |raound| %>
              <td><%= round.scores.sum(:stroke_count) %></td>
            <% end %>
          </tr>
          <tr>
            <th>score</th>
            <% @rounds.each do |raound| %>
              <td><%= round.scores.sum(:put_count) %></td>
            <% end %>
          </tr>
        </tbody>
      </table>
      <% else %>
        <p class="text-center">ラウンド情報はありません</p>
      <% end %>
    </div>
    <div class="col-lg-3">
      <%= link_to "スコア入力", new_user_round_path(@user) %>
    </div>
  </div>
</div>